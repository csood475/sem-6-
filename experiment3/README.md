# multi-page-theme

A minimal Next.js + TypeScript project demonstrating multiple pages that share a common Chakra UI theme.

Run locally (macOS zsh):

1. Install dependencies

   npm install

2. Run dev server

   npm run dev

Where to edit the theme: `theme/index.ts`.
